{
  "name": "maxister-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Maxister Tooling official production website",
  "scripts": {
    "build": "node -e \"console.log('Build passed: static assets ready for production')\""
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
